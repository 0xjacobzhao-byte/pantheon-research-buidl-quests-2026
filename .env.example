# Offline demo needs NO secrets. These are placeholders only — leaving them empty
# keeps every module in offline/cached mode (the default). No live paid LLM call
# is made in the public repo; the five-model cockpit is cached-only.
DASHSCOPE_API_KEY=
DEEPSEEK_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
DEMO_MODE=offline
BACKEND_PORT=8000
FRONTEND_PORT=5173
